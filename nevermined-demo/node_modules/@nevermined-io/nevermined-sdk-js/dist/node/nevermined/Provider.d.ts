import Account from './Account';
import { Instantiable, InstantiableConfig } from '../Instantiable.abstract';
export declare class Provider extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<Provider>;
    add(did: string, address: string, from: Account): Promise<boolean>;
    remove(did: string, address: string, from: Account): Promise<boolean>;
    list(did: string): Promise<any>;
}
