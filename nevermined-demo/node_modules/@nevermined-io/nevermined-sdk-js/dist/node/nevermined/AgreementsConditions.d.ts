import Account from './Account';
import { Instantiable, InstantiableConfig } from '../Instantiable.abstract';
export declare class AgreementsConditions extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<AgreementsConditions>;
    lockReward(agreementId: string, amount: number | string, from?: Account): Promise<boolean>;
    grantAccess(agreementId: string, did: string, grantee: string, from?: Account): Promise<boolean>;
    grantServiceExecution(agreementId: string, did: string, grantee: string, from?: Account): Promise<boolean>;
    releaseReward(agreementId: string, amounts: number[], receivers: string[], did: string, consumer: string, publisher: string, from?: Account): Promise<boolean>;
}
