import { Instantiable, InstantiableConfig } from '../../Instantiable.abstract';
import { JwtUtils } from './JwtUtils';
import { ServiceAgreement } from './ServiceAgreement';
import { SignatureUtils } from './SignatureUtils';
import { WebServiceConnector } from './WebServiceConnector';
export declare class Utils extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<Utils>;
    agreements: ServiceAgreement;
    signature: SignatureUtils;
    fetch: WebServiceConnector;
    jwt: JwtUtils;
}
