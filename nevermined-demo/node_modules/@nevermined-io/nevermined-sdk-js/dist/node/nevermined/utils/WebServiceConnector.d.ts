import { BodyInit, RequestInit, Response } from 'node-fetch';
import { Instantiable, InstantiableConfig } from '../../Instantiable.abstract';
export declare class WebServiceConnector extends Instantiable {
    constructor(config: InstantiableConfig);
    post(url: string, payload: BodyInit, headers?: {
        [header: string]: string;
    }): Promise<Response>;
    get(url: string, headers?: {
        [header: string]: string;
    }): Promise<Response>;
    put(url: string, payload: BodyInit, headers?: {
        [header: string]: string;
    }): Promise<Response>;
    delete(url: string, payload?: BodyInit, headers?: {
        [header: string]: string;
    }): Promise<Response>;
    downloadFile(url: string, destination?: string, index?: number, headers?: any): Promise<string>;
    fetch(url: string, opts: RequestInit): Promise<Response>;
}
