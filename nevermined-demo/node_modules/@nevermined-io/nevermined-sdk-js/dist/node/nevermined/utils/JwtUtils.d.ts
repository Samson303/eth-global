import { Instantiable, InstantiableConfig } from "../../Instantiable.abstract";
import { Account } from "../../../src";
export declare class JwtUtils extends Instantiable {
    GRANT_TYPE: string;
    BASE_AUD: string;
    tokenCache: Map<string, string>;
    constructor(config: InstantiableConfig);
    generateCacheKey(...args: string[]): string;
    accountToJwk(account: Account): Promise<any>;
    generateAccessGrantToken(account: Account, serviceAgreementId: string, did: string): Promise<string>;
    generateDownloadGrantToken(account: Account, did: string): Promise<string>;
    generateExecuteGrantToken(account: Account, serviceAgreementId: string, workflowId: string): Promise<string>;
    generateComputeGrantToken(account: Account, serviceAgreementId: string, executionId: string): Promise<string>;
}
