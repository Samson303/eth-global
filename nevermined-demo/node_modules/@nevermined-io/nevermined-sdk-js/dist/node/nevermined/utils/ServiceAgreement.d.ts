import { DDO } from '../../ddo/DDO';
import Account from '../Account';
import { Instantiable, InstantiableConfig } from '../../Instantiable.abstract';
export declare class ServiceAgreement extends Instantiable {
    constructor(config: InstantiableConfig);
    signServiceAgreement(ddo: DDO, index: number, serviceAgreementId: string, agreementConditionsIds: string[], consumer: Account): Promise<string>;
    createHashSignature(templateId: string, serviceAgreementId: string, valueHashes: string[], timelockValues: number[], timeoutValues: number[], consumer: Account): Promise<string>;
    hashServiceAgreement(serviceAgreementTemplateId: string, serviceAgreementId: string, valueHashes: string[], timelocks: number[], timeouts: number[]): string;
    private getTimeValuesFromService;
}
