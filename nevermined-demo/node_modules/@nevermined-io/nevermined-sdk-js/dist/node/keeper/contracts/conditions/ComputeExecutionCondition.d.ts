import { Condition } from './Condition.abstract';
import { InstantiableConfig } from '../../../Instantiable.abstract';
export declare class ComputeExecutionCondition extends Condition {
    static getInstance(config: InstantiableConfig): Promise<ComputeExecutionCondition>;
    hashValues(did: string, computeConsumer: string): Promise<string>;
    fulfill(agreementId: string, did: string, computeConsumer: string, from?: string): any;
    wasComputeTriggered(did: string, computeConsumer: string, from?: string): Promise<boolean>;
}
