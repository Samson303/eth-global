import { AgreementTemplate } from './AgreementTemplate.abstract';
export declare abstract class BaseEscrowTemplate extends AgreementTemplate {
    createAgreement(agreementId: string, did: string, conditionIds: string[], timeLocks: number[], timeOuts: number[], accessConsumer: string, from?: string): any;
    getAgreementData(agreementId: string): Promise<any>;
}
