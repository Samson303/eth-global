import ContractBase from './ContractBase';
import { InstantiableConfig } from '../../Instantiable.abstract';
export default class Token extends ContractBase {
    static getInstance(config: InstantiableConfig): Promise<Token>;
    approve(to: string, price: number | string, from?: string): Promise<import("web3-core").TransactionReceipt>;
    decimals(): Promise<number>;
    balanceOf(address: string): Promise<number>;
    symbol(): Promise<string>;
    name(): Promise<string>;
    totalSupply(): Promise<number>;
    transfer(to: string, amount: number, from: string): Promise<import("web3-core").TransactionReceipt>;
}
