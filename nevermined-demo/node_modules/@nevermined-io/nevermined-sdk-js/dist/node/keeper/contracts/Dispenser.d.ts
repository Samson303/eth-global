import ContractBase from './ContractBase';
import { InstantiableConfig } from '../../Instantiable.abstract';
export default class Dispenser extends ContractBase {
    static getInstance(config: InstantiableConfig): Promise<Dispenser>;
    requestTokens(amount: number | string, receiverAddress: string): Promise<import("web3-core").TransactionReceipt>;
}
