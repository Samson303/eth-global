import { Condition } from './Condition.abstract';
import { InstantiableConfig } from '../../../Instantiable.abstract';
export declare class LockRewardCondition extends Condition {
    static getInstance(config: InstantiableConfig): Promise<LockRewardCondition>;
    hashValues(rewardAddress: string, amount: number | string): Promise<string>;
    fulfill(agreementId: string, rewardAddress: string, amount: number | string, from?: string): any;
}
