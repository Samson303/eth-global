import { Condition } from './Condition.abstract';
import { InstantiableConfig } from '../../../Instantiable.abstract';
export declare class EscrowReward extends Condition {
    static getInstance(config: InstantiableConfig): Promise<EscrowReward>;
    hashValues(amounts: number[], receivers: string[], sender: string, lockCondition: string, releaseCondition: string): Promise<string>;
    fulfill(agreementId: string, amounts: number[], receivers: string[], sender: string, lockCondition: string, releaseCondition: string, from?: string): any;
}
