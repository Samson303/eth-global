import { Instantiable, InstantiableConfig } from '../Instantiable.abstract';
export declare class Faucet extends Instantiable {
    private get url();
    constructor(config: InstantiableConfig);
    requestEth(address: string): Promise<string>;
    getFaucetEndpoint(): string;
}
