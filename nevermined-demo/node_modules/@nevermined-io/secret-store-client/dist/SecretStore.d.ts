import SecretStoreConfig from "./models/SecretStoreConfig";
export default class SecretStore {
    private parityClient;
    private secretStoreClient;
    constructor(config: SecretStoreConfig);
    encryptDocument(serverKeyId: string, document: any): Promise<string>;
    decryptDocument(serverKeyId: string, encryptedDocument: string): Promise<any>;
    private generateServerKey;
    private generateDocumentKey;
    private storeDocumentKey;
    private retrieveDocumentKey;
}
