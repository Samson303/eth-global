import ParityClientConfig from "./models/ParityClientConfig";
export default class ParityClient {
    private address;
    private password;
    private url;
    constructor(config: ParityClientConfig);
    signKeyId(keyId: any): Promise<string>;
    generateDocumentKeyFromServerKey(serverKey: string): Promise<any>;
    encryptDocument(encryptedKey: string, document: any): Promise<string>;
    decryptDocument(decryptedSecret: string, commonPoint: string, decryptShadows: string[], encryptedDocument: string): Promise<any>;
    private sendJsonRpcRequest;
}
